#include <string>

//Implementation of algorithm to determine if a string has all unique characters. 
//Note : not used additional data structures.
auto check_all_unique_charactersv1(const std::string &param) -> bool;

//Implementation of algorithm to determine if a string has all unique characters. 
//Note : used additional data structures.
auto check_all_unique_charactersv2(const std::string &param) -> bool;