#include <string>

//Implementation of algorithm to determine if a string has all unique characters. 
//Note : you can not use additional data structures.
auto check_all_unique_characters(const std::string &param) -> bool;